<div class="navbar navbar-light">
  <%= link_to root_path do %>
    <%= image_tag("https://res.cloudinary.com/djduvb702/image/upload/v1611908575/Renterpreter_temp-logo_z9dnyr.png", width: 40, height: 40) %>
  <% end %>
  <div class="navbar-right">
    <% if !user_signed_in? %>
      <%= link_to "Become a Translator", new_translator_path, class: "newTranslatorLink"  %>
    <% elsif user_signed_in? && current_user.translator == nil %>
      <%= link_to "Become a Translator", new_translator_path, class: "newTranslatorLink"  %>
    <% elsif user_signed_in? && current_user.translator.id.present? %>
      <%= link_to "Edit Translator Profile", edit_translator_path(current_user.translator.id), class: "newTranslatorLink" %>
    <% else %>
      <%= link_to "Become a Translator", new_translator_path, class: "newTranslatorLink"  %>
    <% end %>
    <!--       We need to replace the placeholder image below with the user's uploaded profile pic -->
         <% if user_signed_in? && current_user.photo.attached? %>
          <%= cl_image_tag current_user.photo.key, class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
        <% else %>
          <%= image_tag "https://res.cloudinary.com/dgxnxohzx/image/upload/v1612061042/interpretation-bot_stl5v4.png", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
        <% end %>
    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
      <ul>
        <li><%= link_to "View Bookings", profile_path, class: "dropdown-item" %></li>
        <li><%= link_to "Edit Translator Profile",  edit_translator_path(:translator_id), class: "dropdown-item" %></li>
        <li><hr class="dropdown-divider"></li>
        <li><%= link_to "Logout", destroy_user_session_path, method: :delete, class: "dropdown-item" %></li>
      </ul>
    </div>
  </div>
</div>